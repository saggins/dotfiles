- defaults:
    link:
        relink: true
        create: true
        force : true 
- clean: ['~', '~/.config/']

- link:
    ~/.bashrc: bash/bashrc
    ~/.vimrc: vim/vimrc
    /etc/nixos/configuration.nix: nixos/configuration.nix
- shell: []
