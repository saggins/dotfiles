- defaults:
    link:
        relink: true
        create: true
        force : true 
- clean: ['~', '~/.config/']

- link:
    ~/.bashrc: bash/bashrc
    ~/.config/i3/config: i3/config
    ~/.vimrc: vim/vimrc
    /etc/nixos/configuration.nix: nixos/configuration.nix
- shell: []
